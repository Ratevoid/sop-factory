# Empty contract Extension Surface

No project knowledge is bundled here. Supply validated contract files through a project-owned `.sop/`
directory, user configuration, or `project configuration`. Keep paths relative where portability matters.
