# Empty Profile Extension Surface

No project knowledge is bundled here. Supply validated Profile files through a project-owned `.sop/`
directory, user configuration, or `SOP_PROFILE_DIRS`. Keep paths relative where portability matters.
